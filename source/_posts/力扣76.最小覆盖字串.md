---
title: 力扣79
description: 
categories:
  - 算法
tags:
  - 力扣
  - 哈希表
date: 2026-09-11 9:09
cover: /img/leetcode.png
---

# [76. 最小覆盖子串](https://leetcode.cn/problems/minimum-window-substring/)

题目描述：

> 给定两个字符串 `s` 和 `t`，长度分别是 `m` 和 `n`，返回 s 中的 **最短窗口 子串**，使得该子串包含 `t` 中的每一个字符（**包括重复字符**）。如果没有这样的子串，返回空字符串 `""`。
>
> 测试用例保证答案唯一。

```python
class Solution:
    def minWindow(self, s: str, t: str) -> str:
        need = {} # 记录需要的字母
        window = {} # 记录当前窗口的有效字母数
        # 先统计需要的每个字母的数量
        for c in t:
            need[c] = need.get(c, 0) + 1
        # 定义窗口
        left = right = 0
        # 定义有效字母的数量
        valid = 0
        # 定义答案的左边界
        start = 0
        # 定义有效字串的长度为无穷大
        length = float('inf')
        while right < len(s):
            # 挨着去right所指的元素
            c = s[right]
            right += 1
            # 判断是否需要
            if c in need:
                # 如果需要，窗口有效值+1
                window[c] = window.get(c,0) + 1
                if window[c] == need[c]:
                    valid += 1
            # 收缩窗口
            while valid == len(need):
                # 更新最小窗口
                if right - left < length:
                    start = left
                    length = right - left
                d = s[left]
                left += 1
                if d in need:
                    # 如果每个有效字母都是一个
                    if window[d] == need[d]:
                        valid -= 1
                    # 这个有效字母重复了不影响，valid不用减，只窗口这个字母对应的数量减就行
                    window[d] -= 1
        if length == float('inf'):
            return ""
        return s[start:start+length]

```

视频讲解链接：[最小覆盖子串 | LeetCode 76_哔哩哔哩_bilibili](https://www.bilibili.com/video/BV1m4isBBE69?spm_id_from=333.788.videopod.sections&vd_source=9eb2c53e5746fd9db6ff7c957a0126ce)