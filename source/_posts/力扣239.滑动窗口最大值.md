---
title: 力扣239
description: 滑动窗口最大值使用双端队列
categories:
  - 算法
tags: 
  - 力扣
  - 双端队列
date: 2026-9-10 10:30
cover: /img/leetcode.png
---

# [239. 滑动窗口最大值](https://leetcode.cn/problems/sliding-window-maximum/)

题目描述：

> 给你一个整数数组 `nums`，有一个大小为 `k` 的滑动窗口从数组的最左侧移动到数组的最右侧。你只可以看到在滑动窗口内的 `k` 个数字。滑动窗口每次只向右移动一位。
>
> 返回 *滑动窗口中的最大值* 。

```python
class Solution:
    def maxSlidingWindow(self, nums: List[int], k: int) -> List[int]:
        ans = []
        if not nums or k == 0:
            return []
        dq = deque()
        for i ,num in enumerate(nums):
            # 移除队尾所有小于等于当前元素的值
            while dq and nums[dq[-1]]<= num:
                dq.pop()
            dq.append(i)
            # 移除窗口左边界外的对头元素
            if dq[0] <= i -k:
                dq.popleft()
            # 窗口形成后，对头为当前窗口的最大值
            if i >= k-1:
                ans.append(nums[dq[0]])
        return ans
```

哔站优质讲解链接：

[滑动窗口最大值 | LeetCode 239_哔哩哔哩_bilibili](https://www.bilibili.com/video/BV1jzzjBuE64/?spm_id_from=333.337.search-card.all.click)